# Copy this file to `.env` and fill in real values.
# `.env` is git-ignored; never commit it.

ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: override the exposed port (defaults to 8000)
PORT=8000